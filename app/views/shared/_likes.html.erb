<% if user_signed_in? %>
  <% if work.like_user(current_user.id) %>
    <%= button_to like_path(work, like), class: "today", method: :delete, remote: true do %>
      <i class="fas fa-heart"></i>
      <span>
        <%= work.likes_count %>
      </span>
    <% end %>
  <% else %>
    <%= button_to work_likes_path(work), class: "today", remote: true do %>
      <i class="far fa-heart"></i>
      <span>
        <%= work.likes_count %>
      </span>
    <% end %>
  <% end %>
<% else %>
  <i class="far fa-heart"></i>
  <span>
    <%= work.likes_count %>
  </span>
<% end %>